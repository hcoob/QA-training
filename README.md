
# QA-training

Summary:
1. Set up the SSH key on our local machine
2. On github we added the .pub key
3. created a bew repo in guthub
4. cloned repo cia the ssh url (not https)

commands to learn:

--CLI:
cd
ls
mkdir
pwd
cat

-- Git:
git clone <url>

git add <files>
git commint -m "your message here"
git push





